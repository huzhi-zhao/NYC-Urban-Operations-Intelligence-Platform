# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Local variable overrides — never commit real values
terraform.tfvars

# Service account keys - NEVER commit these
keys/
*.json
!keys/.gitignore